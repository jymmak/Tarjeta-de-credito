Pseudoc�digo
     ---------------

1.-  Se crea una variable llamada creditCard cuyo valor ser� el n�mero de la tarjeta de cr�dito que el usuario ingrese mediante un prompt.
var creditCard = prompt("Ingrese el n�mero de su tarjeta");
creditCard!== "" && creditCard !== " "
2.-  Se valida los datos
si es verdad  : isValidCard(creditCard)
sino aparece un alert("Ingrese un n�mero v�lido, por favor.");
3.-  Contin�a si cumple creditCard === "" || creditCard=== " "
Mientras el campo est� vac�o el prompt le vuelve a pedir al usuario que ingrese un n�mero de tarjeta de credito.

4.- isValidCard(numbersCard) , esta funci�n verificar� si los n�meros de la tarjeta de cr�dito son v�lidos al Algoritmo de Luhn.
5.- var arr = [];    creamos un array
6.- var size = numbersCard.length;   nos da el tama�o en n�mero de caracteres.
7.- var lastPosition = size - 1;     nos da la �ltima posici�n.
8.- var sumtotal = 0;

9.- si : i>=0  for(var i = lastPosition ; i >= 0 ; i--)
arr.push(parseInt(numbersCard[i])); guarda el  n�mero impar.

10.- Si : var j=1 y j <= arr.length, contin�a
(j % 2) === 0 si es verdad contin�a si es falso te env�a a j++:
var number = arr[j-1] * 2;    guarda el  n�mero par, contin�a
number >= 10 , en caso que multiplicaci�n sea mayor igual a 10.
si es verdad entonces number = number.toString();
contin�a con: var digitOne = parseInt(number[0]);  0 y 1 porque es imposible.
luego segundo d�gito : var digitTwo = parseInt(number[1]);  number sera un n�mero.
contin�a con: arr[j-1] = digitOne + digitTwo;  de m�s d�gitos terminando en j++.
Si number >= 10 es  falso nos da arr[j-1] = number terminando en j++ regresando a j<=arr.length donde j sea  menor que la longitud del array "arr",
11.-  Si j<=arr.length es false creamos una nueva variable  llamada  "k" var k = 0 ; k < arr.length si es verdad
sumtotal = sumtotal + arr[k] ;  Sumatoria de todos los numeros de la tarjeta.

Si es falso sumtotal % 10 === 0
Si es  verdad me devuelve un alert("El n�mero de su tarjeta es v�lido");
Si es falso sumtotal % 10 !== 0
si es falso me devuelve un alert("Lo sentimos, el n�mero de su tarjeta no es v�lido");

12.- isValidCard(); Finalente llamos a la funcion.
